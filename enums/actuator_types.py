from enum import Enum

class ActuatorTypes(Enum):

    # Current actuator types available
    GRABBER = "Picks up items in the virtual environment"
    PLACER = "Places items in the virtual environment"
